<Page x:Class="CellTool.Views.AutomationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:CellTool.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:AutomationViewModel}"
      Title="Automation">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Automation Modes" Margin="0,0,0,12">
            <StackPanel Margin="8">
                <ui:ToggleSwitch Content="File Monitor"
                                 IsChecked="{Binding FileMonitorEnabled}"
                                 Margin="0,4" />
                <ui:ToggleSwitch Content="HTTP API"
                                 IsChecked="{Binding ApiEnabled}"
                                 Margin="0,4" />
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="HTTP API Configuration" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Port:" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" Text="{Binding ApiPort}" Margin="8,0" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="11">
                    <Run Text="Endpoint: http://localhost:" />
                    <Run Text="{Binding ApiPort}" />
                    <Run Text="/analyze" />
                </TextBlock>
            </Grid>
        </GroupBox>

        <Border Grid.Row="2" Padding="16" CornerRadius="6"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap" FontSize="13" />
        </Border>
    </Grid>

</Page>
