<Page x:Class="CellTool.Views.DataConfigPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:CellTool.ViewModels"
      xmlns:models="clr-namespace:CellTool.Models"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:DataConfigViewModel}"
      Title="Data Config">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Chip Database (Excel)" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Excel File:" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ExcelFilePath, UpdateSourceTrigger=PropertyChanged}" Margin="4" />
                <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Width="80"
                        Command="{Binding LoadExcelCommand}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Chip Model:" VerticalAlignment="Center" Margin="0,4,0,0" />
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="4,4,4,0"
                          ItemsSource="{Binding AvailableChips}"
                          SelectedItem="{Binding SelectedChip}"
                          DisplayMemberPath="DieName" />
                <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding ChipStatus}" Margin="4,4,0,0"
                           VerticalAlignment="Center" TextWrapping="Wrap" FontSize="11" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="GroupModel" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Model File:" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding GroupModelFilePath, UpdateSourceTrigger=PropertyChanged}" Margin="4" />
                <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Width="80"
                        Command="{Binding LoadGroupModelCommand}" />

                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding GroupModelStatus}" Margin="4,4,0,0" FontSize="11" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Gray Code Configuration" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="MSB:" VerticalAlignment="Center" />
                <ComboBox Grid.Column="1" Margin="4,0"
                          SelectedItem="{Binding GrayCodeMsb}">
                    <ComboBoxItem Content="U - Upper Page" />
                    <ComboBoxItem Content="M - Middle Page" />
                    <ComboBoxItem Content="L - Lower Page" />
                </ComboBox>

                <TextBlock Grid.Column="2" Text="LSB:" VerticalAlignment="Center" Margin="16,0,4,0" />
                <ComboBox Grid.Column="3" Margin="4,0"
                          SelectedItem="{Binding GrayCodeLsb}">
                    <ComboBoxItem Content="U - Upper Page" />
                    <ComboBoxItem Content="M - Middle Page" />
                    <ComboBoxItem Content="L - Lower Page" />
                </ComboBox>

                <TextBlock Grid.Column="4" Margin="16,0,0,0" VerticalAlignment="Center">
                    <Run Text="Codeword Size:" />
                    <Run Text="{Binding CodewordBytes}" FontWeight="Bold" />
                    <Run Text=" bytes" />
                </TextBlock>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Reference File (for Codeword Comparison)" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Reference File:" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" Text="{Binding ReferenceFilePath, UpdateSourceTrigger=PropertyChanged}" Margin="4" />
                <Button Grid.Column="2" Content="Browse..." Width="80"
                        Command="{Binding BrowseReferenceCommand}" />
            </Grid>
        </GroupBox>
    </Grid>

</Page>
